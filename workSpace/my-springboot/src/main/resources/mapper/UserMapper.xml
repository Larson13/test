<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.testfan.myspringboot.dao.UserDao">


    <insert id="insert" parameterType="cn.testfan.myspringboot.model.User">
        insert into user (id, user_name, password, age)
        values (#{id}, #{userName}, #{password}, #{age})
    </insert>

    <select id="select" parameterType="java.lang.Integer" resultType="cn.testfan.myspringboot.model.User">
        select id, user_name, password, age from user where id = #{id}
    </select>

    <update id="update" parameterType="cn.testfan.myspringboot.model.User">
        update user set user_name = #{userName}, password = #{password} where id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Integer">
        delete from user where id = #{id}
    </delete>


</mapper>